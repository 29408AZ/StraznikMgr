<UserControl x:Class="ModuleListy.Views.ListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="Auto" Height="Auto" >
    <UserControl.Resources>
        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Wskaźnik ładowania -->
        <Border Grid.Row="0" 
                Background="#FFF8DC" 
                Padding="5" 
                Margin="5,5,5,0"
                CornerRadius="3"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <StackPanel Orientation="Horizontal">
                <ProgressBar IsIndeterminate="True" Width="100" Height="16" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Lista Funkcjonariuszy -->
        <Expander Header="Funkcjonariusze" 
                  Grid.Row="1" 
                  Margin="5" 
                  MinWidth="150"
                  IsExpanded="True">
            <ListBox ItemsSource="{Binding Marynarze, UpdateSourceTrigger=PropertyChanged}"
                    DisplayMemberPath="Funkcjonariusze"
                    SelectedItem="{Binding SelectedMarynarz, Mode=TwoWay}"
                    ItemContainerStyle="{StaticResource ListBoxItemStyle}">
            </ListBox>
        </Expander>

        <!-- Lista Jednostek -->
        <Expander Header="Jednostki pływające" 
                  Grid.Row="2" 
                  Margin="5" 
                  MinWidth="150"
                  IsExpanded="True">
            <ListBox ItemsSource="{Binding Jednostki, UpdateSourceTrigger=PropertyChanged}"
                    DisplayMemberPath="Jednostki"
                    SelectedItem="{Binding SelectedJednPlyw, Mode=TwoWay}"
                    ItemContainerStyle="{StaticResource ListBoxItemStyle}">
            </ListBox>
        </Expander>

        <!-- Status bar -->
        <TextBlock Grid.Row="3" 
                   Text="{Binding StatusMessage}" 
                   Margin="10,2,5,5" 
                   FontSize="11" 
                   Foreground="Gray"/>
    </Grid>
</UserControl>